local +r -a Funcs

if [[ $@# > 0 ]]
then
  print "$(green Un-functioning functions:) $(yellow $@)"
  Funcs=($*)
else
  green "Un-functionning all functions"
  Funcs=($FUNCS_HOME/*(:t))
fi

for func in $Funcs
do
  unfunction $func 2> /dev/null
done
