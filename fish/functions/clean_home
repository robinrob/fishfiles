local +r pwd=$PWD

check_cd_root_arg $1 || return 1

IFS="
"

local +r Result
Result=($(gfind . -maxdepth 1 \( -type d -and -regex '.*/[a-z0-9].*' \) -or \( -type f -and -regex '.*/[^.].*' \)))

move_to_trash $Result

cd $pwd > /dev/null

return 0
