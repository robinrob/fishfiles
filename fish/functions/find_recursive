local +r Pattern=$1
[[ -n $2 ]] && local +r Where=$2 || local +r Where='.'
local +r MaxDepth=$3

if (( MaxDepth > 0 ))
then
  cmd="gfind $Where -maxdepth $MaxDepth -iname '*$Pattern*'"
else
  cmd="gfind $Where -iname '*$Pattern*'"
fi

green $cmd
eval $cmd
