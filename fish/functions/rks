IFS=' '

# case $(basename $(pwd)) in
  # robinrob.github.io-dev) TASK="jekyll:save" ;;
  # *) TASK="base:save" ;;
# esac
TASK="base:save"

rake_do $TASK "$*"

# if [[ $(is_in_set $*[-1] 'bitbucket github origin live') == "yes" ]]
# then
#   rake_do $TASK $*[1,-2] $*[-1]
# else
# fi

