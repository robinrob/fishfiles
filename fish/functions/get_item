local +r Record=$1
shift; local -a Files; Files=($*)

for file in $Files
do
  row=$(pcregrep -o1 -M -N CRLF "($Record(::[^\n]+))" $file)
  row=(${(s/::/)row})
  val=$row[-1]

  [[ $(is_empty $val) == "no" ]] && break
done

[[ $(is_empty $val) == "no" ]] && copy_print ""$val""
