case $#@ in
  1)
    grep -ri $1 * ;;
  2)
    find $2 -type f -exec printf {}: \; -exec grep -H $1 {} \; ;;
  3)
    find $2 -maxdepth $3 -type f -exec grep -H $1 {} \; ;;
  *)
    red "grep_recursive <regex> <location> <depth>" ;;
esac
